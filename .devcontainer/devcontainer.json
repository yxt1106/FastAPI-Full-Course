{
  "name": "FastAPI Full Course",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/FastAPI-Full-Course",
  "shutdownAction": "stopCompose",
  "overrideCommand": false,
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "FastAPI",
      "onAutoForward": "openBrowser"
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12",
      "installTools": true,
      "toolsToInstall": "uv"
    }
  },
  "postCreateCommand": "uv sync",
  "postStartCommand": "uv run alembic upgrade head && (nohup uv run fastapi dev --host 0.0.0.0 --port 8000 > /tmp/fastapi.log 2>&1 &)",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
      }
    }
  }
}
